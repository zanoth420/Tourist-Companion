ortools>=9.10
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0

# production data layer (used only when DATABASE_URL / REDIS_URL are set)
psycopg[binary,pool]>=3.1
redis>=5.0

# dev / test
pytest>=8.0
httpx>=0.27
